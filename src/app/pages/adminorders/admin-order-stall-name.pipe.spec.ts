import { AdminOrderStallNamePipe } from './admin-order-stall-name.pipe';

describe('AdminOrderStallNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrderStallNamePipe();
    expect(pipe).toBeTruthy();
  });
});
