import { AdminOrderCanteenPipe } from './admin-order-canteen.pipe';

describe('AdminOrderCanteenPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrderCanteenPipe();
    expect(pipe).toBeTruthy();
  });
});
