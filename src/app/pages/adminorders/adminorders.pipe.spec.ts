import { AdminordersPipe } from './adminorders.pipe';

describe('AdminordersPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminordersPipe();
    expect(pipe).toBeTruthy();
  });
});
