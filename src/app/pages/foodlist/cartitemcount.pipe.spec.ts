import { CartitemcountPipe } from './cartitemcount.pipe';

describe('CartitemcountPipe', () => {
  it('create an instance', () => {
    const pipe = new CartitemcountPipe();
    expect(pipe).toBeTruthy();
  });
});
