import { CartTotalCostPipe } from './cart-total-cost.pipe';

describe('CartTotalCostPipe', () => {
  it('create an instance', () => {
    const pipe = new CartTotalCostPipe();
    expect(pipe).toBeTruthy();
  });
});
