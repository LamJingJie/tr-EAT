import { OrdermonthlyNamePipe } from './ordermonthly-name.pipe';

describe('OrdermonthlyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdermonthlyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
