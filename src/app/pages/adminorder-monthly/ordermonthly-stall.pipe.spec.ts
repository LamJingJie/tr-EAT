import { OrdermonthlyStallPipe } from './ordermonthly-stall.pipe';

describe('OrdermonthlyStallPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdermonthlyStallPipe();
    expect(pipe).toBeTruthy();
  });
});
